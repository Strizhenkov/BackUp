namespace Backups;

public class Storage
{
    private string pathToBackup; //  путь к сохраненному файлу

    public Storage(string pathToObject, string code)
    {
        public byte[] getContents(pathToObject);
        public byte[] getAllLines(pathToObject);
        string algorithm = code;
    }

    public string algorithm { get; }
}

